# === 阿斯拉量化系統 - 後端依賴 ===

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
websockets>=13.0,<16.0

# 數據處理
# 註：pandas 3 / numpy 2 為現行可用組合。pandas-ta 必須用 0.4.x（相容 numpy 2），
# 舊的 0.3.14b1 依賴已被 numpy 2 移除的 numpy.NaN，會在 import 時直接失敗。
# 精確重現請用 requirements.lock.txt。
pandas>=3.0,<3.1
numpy>=2.2,<3

# 技術指標計算（0.4.x fork，相容 numpy 2 / pandas 3）
pandas-ta==0.4.71b0

# 統計計算（因子分析）
scipy>=1.13,<2

# 時序分解（STL）
statsmodels>=0.14,<0.15

# 加密貨幣交易所 API
ccxt>=4.4.0

# 台股數據
yfinance>=1.2.0

# HTTP（tw_universe / run.py 直接使用，先前漏列）
requests>=2.31.0

# LLM 供應商 SDK
openai>=1.50.0
anthropic>=0.37.0
google-genai>=1.0.0
httpx>=0.27.0

# 加密 (API Key 存儲)
cryptography>=43.0.0

# 環境變數管理
python-dotenv>=1.0.1

# 時區
pytz>=2024.1

# 資料驗證
pydantic>=2.9.0
pydantic-settings>=2.5.0

# 日誌
loguru>=0.7.0

# ML 增強模組
lightgbm>=4.0.0
xgboost>=2.0.0
scikit-learn>=1.3.0

# ML 增強模組（可選 — 神經網路模型）
# torch>=2.0.0

# 測試
pytest>=8.3.0
pytest-asyncio>=0.24.0

# 語意嵌入（本地模型，語意快取用）
sentence-transformers>=4.0.0

# PDF 生成（知識報告匯出）
fpdf2>=2.8.0
